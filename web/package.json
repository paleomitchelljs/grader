{
  "name": "grader-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based OMR scanner for bubble-sheet exams. Static. FERPA-safe by architecture.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@techstark/opencv-js": "^4.10.0-release.1",
    "papaparse": "^5.4.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.15",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
